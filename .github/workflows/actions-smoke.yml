name: Actions Smoke Test

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm runner starts
        run: echo "GitHub Actions is able to queue and run workflows."
